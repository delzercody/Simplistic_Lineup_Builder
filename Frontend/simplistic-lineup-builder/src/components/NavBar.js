// NavBar.js

import React from 'react';

function NavBar() {
    return (
        <div>
            <h1>This is the Nav Bar which should be on every page of the application</h1>
        </div>
    );
}

export default NavBar;
