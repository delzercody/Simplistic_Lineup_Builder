// SavedLineupCard.js

import React from 'react';

function SavedLineupCard() {
    return (
        <div>
            <h1>This represents a full saved lineup that can be clicked to edit or delete. It will be shown through the saved lineup list on the view saved lineups page.</h1>
        </div>
    );
}

export default SavedLineupCard;
